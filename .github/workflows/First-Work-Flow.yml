name: my-first-flow
on: push
jobs:
  run-command-shell:
    runs-on: ubuntu-latest
    steps:
      - name: "Echo string"
        run: eccho "Hello world"
      - name: "Multiline echo"
        run: |
          node -v
          npm -v


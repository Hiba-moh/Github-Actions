name: First-Java-Actions
on: [push]
jobs:
  First-Job:
    runs-on: ubuntu-latest
    steps:
      - name: simple jvascript action
        id: greet

        uses: Hiba-moh/JavaScriptAction@v1
        with:
          who-to-greet: Hiba1
      - name: log greeting time
        run: print "${{steps.greet.outputs.time}}"
